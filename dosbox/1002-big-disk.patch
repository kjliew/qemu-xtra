diff -rwu ../orig/r4095/include/bios_disk.h ./include/bios_disk.h
--- ../orig/r4095/include/bios_disk.h	2018-05-01 21:29:55 -0700
+++ ./include/bios_disk.h	2018-06-10 02:05:27 -0700
@@ -66,10 +66,15 @@
 	Bit32u sector_size;
 	Bit32u heads,cylinders,sectors;
 private:
-	Bit32u current_fpos;
+	Bit64u current_fpos;
 	enum { NONE,READ,WRITE } last_action;
 };
 
+#ifndef __APPLE__
+#define fseek fseeko64
+#define ftell ftello64
+#endif
+
 void updateDPT(void);
 void incrementFDD(void);
 
diff -rwu ../orig/r4095/src/ints/bios_disk.cpp ./src/ints/bios_disk.cpp
--- ../orig/r4095/src/ints/bios_disk.cpp	2018-05-01 21:29:47 -0700
+++ ./src/ints/bios_disk.cpp	2018-06-10 01:34:43 -0700
@@ -159,9 +159,9 @@
 }
 
 Bit8u imageDisk::Read_AbsoluteSector(Bit32u sectnum, void * data) {
-	Bit32u bytenum;
+	Bit64u bytenum;
 
-	bytenum = sectnum * sector_size;
+	bytenum = (Bit64u)sectnum * sector_size;
 
 	if (last_action==WRITE || bytenum!=current_fpos) fseek(diskimg,bytenum,SEEK_SET);
 	size_t ret=fread(data, 1, sector_size, diskimg);
@@ -181,9 +181,9 @@
 
 
 Bit8u imageDisk::Write_AbsoluteSector(Bit32u sectnum, void *data) {
-	Bit32u bytenum;
+	Bit64u bytenum;
 
-	bytenum = sectnum * sector_size;
+	bytenum = (Bit64u)sectnum * sector_size;
 
 	//LOG_MSG("Writing sectors to %ld at bytenum %d", sectnum, bytenum);
 
